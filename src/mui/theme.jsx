import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#4a148c", // set the primary color to dark purple
    },
    secondary: {
      main: "#000000", // set the secondary color to black
    },
  },
});

export default theme;
